#include <stdio.h>

int main(void)
{

    int count =0;
    long old=0;
    long prev=1;

    while (count<50);
    printf("%lu", old+ prev);
old=prev;
prev = current;
    putchar(',');
    count++;
}
    ptrintf ("The size of long %ld",sizeof(x));
    putchar('\n');
    return (0);
}
